import React, { useContext } from "react";
import { Container, Navigation, Input, Textarea, Button, Notification } from "../../components";


const Test = (props) => {
    
    return (

            <div className="columns">
                <aside id="view-page-aside" className="column aside hero is-2x is-fullheight">
                <Navigation activeUrl="/diagrams"></Navigation>
                </aside>
            </div>

    );
};

export default Test;